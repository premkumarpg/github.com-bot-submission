apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo-service.fullname" . }}-config
  labels:
    {{- include "demo-service.labels" . | nindent 4 }}
data:
  APP_NAME: {{ .Values.config.appName | quote }}
  VERSION: {{ .Values.config.version | quote }}
